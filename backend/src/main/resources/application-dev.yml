spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: "https://dev-api.khuoo.synology.me/login/oauth2/code/google"

management:
  endpoint:
    health:
      show-details: never

app:
  auth:
    cookie-domain: .khuoo.synology.me
    redirect-url: "https://dev-api.khuoo.synology.me/swagger-ui/index.html"
  dev-token:
    enabled: false
  storage:
    root: /mnt/gradmanager-files
  jwt:
    secret: "${JWT_SECRET}"
    exp-seconds: 604800  # 7일
  swagger:
    server-url: https://dev-api.khuoo.synology.me