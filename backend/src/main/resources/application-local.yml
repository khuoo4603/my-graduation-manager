spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/gradmanager
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

app:
  dev-token:
    enabled: true
  storage:
    root: /tmp/gradmanager_local_storage